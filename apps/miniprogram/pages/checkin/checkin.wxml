<view class="container">
  <view wx:if="{{loading}}" class="empty">Loading meeting…</view>
  <block wx:else>
    <view class="empty">{{message || 'Sign in to view this meeting.'}}</view>
    <button class="btn" bindtap="viewMeeting">View meeting</button>
  </block>
</view>
