.container { padding-bottom: 168rpx; }

.vote-head { display: flex; flex-direction: column; gap: 8rpx; }
.vote-title { font-size: 34rpx; font-weight: 700; }
.vote-sub { font-size: 26rpx; color: #6b6b70; }

.empty-state { text-align: center; padding: 40rpx 24rpx; }
.group-title { display: block; font-size: 30rpx; font-weight: 600; margin-bottom: 16rpx; }

.vote-group { margin-bottom: 20rpx; }
.candidate {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 16rpx 8rpx;
  border-top: 1rpx solid #f0f0f2;
}
.candidate:first-child { border-top: none; }
.candidate-active { background: #f6f1fb; border-radius: 12rpx; }

.dot {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 30rpx;
  height: 30rpx;
  border-radius: 50%;
  border: 2rpx solid #b8b8be;
  background: #fff;
  flex: 0 0 auto;
}
.dot-active { border-color: #5b2a86; background: #fff; }
.dot-inner {
  width: 10rpx;
  height: 10rpx;
  border-radius: 50%;
  background: transparent;
}
.dot-inner-active { background: #5b2a86; }

.candidate-main { display: flex; flex: 1; min-width: 0; flex-direction: column; }
.candidate-name { font-size: 30rpx; overflow-wrap: break-word; }
.candidate-role { font-size: 24rpx; margin-top: 2rpx; }

.savebar {
  position: fixed;
  left: 50%;
  bottom: 0;
  width: 100%;
  max-width: 600px;
  transform: translateX(-50%);
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 16rpx 24rpx calc(16rpx + env(safe-area-inset-bottom));
  background: #fff;
  border-top: 1rpx solid #ececee;
  z-index: 20;
}
.save-hint { flex: 1; min-width: 0; font-size: 22rpx; overflow-wrap: break-word; }
.save-btn { flex: 0 0 auto; padding: 20rpx 34rpx; }
.result-btn {
  flex: 0 0 auto;
  background: #fff;
  color: #5b2a86;
  border: 2rpx solid #5b2a86;
  padding: 18rpx 28rpx;
}
.save-btn.disabled { opacity: 0.5; }

@media (max-width: 360px) {
  .save-hint { display: none; }
  .savebar { justify-content: flex-end; gap: 10rpx; }
  .save-btn { padding-left: 24rpx; padding-right: 24rpx; }
  .result-btn { padding-left: 20rpx; padding-right: 20rpx; }
}
