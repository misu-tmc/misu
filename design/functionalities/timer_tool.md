# Timer's tool

This is a util page to help timer to record/warn the time consumed for each session in agenda.

For example (not determined):
- Add a button for each session to start/stop recording.
- Change color when current session past the threshold.
- Beap when the session exceeds time.

UI:
- Focus on and highlight the current session. Might be full screen.
- When the current session ends, auto shift focus to the next session.
- Show button to start the timer for the current session.